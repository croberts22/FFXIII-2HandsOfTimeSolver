git_url(ENV.fetch('MATCH_GIT_URL', 'git@github.com:croberts22/ios-certificates.git'))

type('appstore')

app_identifier(ENV.fetch('APP_IDENTIFIER', 'com.coreyroberts.ffxiii2solver'))
username(ENV.fetch('FASTLANE_USER', 'corey@spacepyro.com'))
team_id(ENV.fetch('FASTLANE_TEAM_ID', 'QYFSMCG7FR'))

# For all available options run `match --help`.
